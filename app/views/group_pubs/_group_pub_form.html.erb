<%= form_for [group, group_pub] do |f| %>
    <%= f.hidden_field :group_id %>
    <%= f.text_area :content, as: :text, class:"my-input", placeholder:"Type your post"%>
    <%= f.submit "Share on group", class:"btn btn-primary"%>
<% end %>